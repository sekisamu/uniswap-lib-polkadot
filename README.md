# uniswap-lib-polkadot

Solidity libraries that are shared across Uniswap contracts on Polkadot. This package focuses on safety and execution gas efficiency.

## Install
Run `pnpm install` to install all deps.